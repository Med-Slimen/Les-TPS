#include <stdio.h>
#include <stdlib.h>
#include<math.h>
void main()
{
    /*
int k,i,f,j,x,s;
printf("donner k :");
scanf("%d",&k);
printf("donner x :");
scanf("%d",&x);
s=0;
    for(i=1;i<=k;i++){
            f=1;
            for(j=2;j<=2*k+1;j++){
                f=f*j;
            }
            s=s+(pow(-1,k)*(pow(x,2*k)+1/f));
  }
  printf("la somme S%d est %d : ",k,s);
  }
  */
