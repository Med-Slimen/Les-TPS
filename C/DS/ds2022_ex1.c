#include <stdio.h>
#include <stdlib.h>
#include <math.h>
/*
int compter(char password[20],int n){
    int i,x;
    x=0;i=0;
    do{
        if(password[i]>='0' && password[i]<='9' ){
            x++;
        }
        i++;
    }
    while(i<n && x<2);
    return x;

}
void main()
{
    char password[20];
    int n,i;
    do{
        printf("donner n : ");
        scanf("%d",&n);
    }
    while(n<6 || n>20);
    printf("donner le mot de passe : ");
    scanf("%s",password);
    if(compter(password,n)>=2){
        printf("Le mot de passe est bon");
    }
    else{
        printf("Le mot de passe doit contenir au moin 2 chiffres");
    }


}*/
