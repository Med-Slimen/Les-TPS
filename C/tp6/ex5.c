#include <stdio.h>
#include <string.h>
/*
int main() {
    int i, pos;
    char ph[50], alpha[27], code[50] = "";
    char ptr;
    printf("donner une phrase : ");
    scanf("%s", ph);
    strcpy(code,ph);
    for (i = 0; i < strlen(ph); i++) {
        if(ph[i]=='Z'){
            code[i]='A';
        }
        else{
            code[i]=(char)(ph[i]+1);
        }
    }
    printf("La variable phrase contient : %s\n", ph);
    printf("La variable résultat code contient : %s\n", code);

    return 0;
}*/
