#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/*
void affiche(char ch[50]){
    int i=0,e=0,o=0,a=0,u=0,j;
    for(j=0;j<strlen(ch);j++){
        switch(ch[j]){
            case 'i':i++;break;
            case 'e':e++;break;
            case 'o':o++;break;
            case 'a':a++;break;
            case 'u':u++;break;
            default:break;
        }
    }
    printf("La voyelle << %c >> apparait %d fois \n",'i',i);
    printf("La voyelle << %c >> apparait %d fois \n",'e',e);
    printf("La voyelle << %c >> apparait %d fois \n",'o',o);
    printf("La voyelle << %c >> apparait %d fois \n",'a',a);
    printf("La voyelle << %c >> apparait %d fois \n",'u',u);

}
int main()
{
    char ch[50];
    printf("donner ch : ");
    scanf("%s",ch);
    affiche(ch);
}*/
