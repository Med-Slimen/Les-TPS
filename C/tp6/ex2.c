#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*

void main()
{
    int i;
    char ch[50];
    printf("donner ch : ");
    scanf("%s",ch);
    i=0;
    while(i<=strlen(ch)/2 && ch[i]==ch[strlen(ch)-1-i]){
        i++;
          };
    if(i-1==strlen(ch)/2){
        printf("palindrome");
    }
    else{
        printf("n est pas palindrome");
    }
}
*/
