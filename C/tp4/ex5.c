#include <stdio.h>
#include <stdlib.h>
/*
void main()
{
    int m[5][4];
    int i,j,n,l,s,ind,max;
    do{
        printf("n = ");
        scanf("%d",&n);
        printf("l = ");
        scanf("%d",&l);
    }
    while(n<0 || l<0);

    for(i=0;i<n;i++){
        for(j=0;j<l;j++){
            printf("m[%d,%d] =  ",i,j);
            scanf("%d",&m[i][j]);
        }
    }
    for(i=0;i<n;i++){
        for(j=0;j<l;j++){
            printf("%d | ",m[i][j]);
        }
        printf("\n");
    }
    for(j=0;j<l;j++){
        max=max+m[0][j];
        }
    ind=0;
    for(i=1;i<n;i++){
        s=0;
        for(j=0;j<l;j++){
            s=s+m[i][j];
        }
        if(s>max){
            ind=i;
            max=s;
        }

    }
    printf("la ligne qui a le max est %d avec un max : %d ",ind,max);
}*/
