/*#include <stdio.h>
#include <stdlib.h>

int addition(a,b)
{
    return a+b;
}
int soustraction(a,b)
{
    return a-b;
}
int multipication(a,b)
{
    return a*b;
}
int division(a,b)
{
    return a/b;
}
void main(){
    int b,a;
    char op;
    printf("donner a : ");
    scanf("%i",&a);
    printf("donner b : ");
    scanf("%i",&b);
    printf("donner l op : ");
    scanf(" %c",&op);
    switch(op){
        case '+':printf("%d + %d = %d",a,b,addition(a,b));break;
        case '-':printf("%d - %d = %d",a,b,soustraction(a,b));break;
        case '*':printf("%d * %d = %d",a,b,multipication(a,b));break;
        case '/':printf("%d / %d = %d",a,b,division(a,b));break;
    };

}*/
